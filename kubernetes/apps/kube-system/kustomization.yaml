---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system

components:
  - ../../components/alerts

resources:
  - ./namespace.yaml
  - ./cilium/ks.yaml
  - ./coredns/ks.yaml
  - ./descheduler/ks.yaml
  - ./drm-exporter/ks.yaml
  - ./kata-containers/ks.yaml
  - ./local-path-provisioner/ks.yaml
  - ./metrics-server/ks.yaml
  # Temporary: CSI VolumeSnapshots unused (Kopiur copyMethod: Direct).
  # Re-enable: uncomment + clear suspend on snapshot-controller/ks.yaml
  # - ./snapshot-controller/ks.yaml
  - ./reloader/ks.yaml
  - ./spegel/ks.yaml
