---
# MariaDB data — keep local (fast queries). PhotoPrism does not support PostgreSQL.
# Originals/import on NFS HDD; cache/thumbs on NFS NVMe; only this DB needs a local volume.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photoprism-db
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
